/**
 * 端末
 */
enum Device {
  pc = 'pc',
  ios = 'ios',
  android = 'android',
}

/**
 * 言語
 */
enum Lang {
  ja = 'ja',
  en = 'en',
}

const circuit_max_g = 1.4
const touge_max_g = 1.0
export { Device, Lang, circuit_max_g, touge_max_g }
